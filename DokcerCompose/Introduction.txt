
first let us run the container

create the docker network

    - docker network create { my-network }

    - docker run --name mongo-dc -d -p 27017:27017 -e MONGO_INITDB_ROOT_USERNAME=admin -e MONGO_INITDB_ROOT_PASSWORD=password --network dc mongo

    - docker run -d -p 8081:8081 --name express-dc -e ME_CONFIG_MONGODB_ADMINUSERNAME=admin -e ME_CONFIG_MONGODB_ADMINPASSWORD=password -e ME_CONFIG_MONGODB_SERVER=mongo-dc --network dc mongo-express

    - to login use { docker logs [container ID ]}~
